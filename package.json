{
  "name": "turing-shortbus",
  "version": "0.0.1",
  "scripts": {
    "clean": "rm -rf build",
    "copyhtml": "cp ./src/index.html ./build/index.html",
    "pretsc": "npm run clean",
    "tsc": "tsc",
    "prewebpack": "npm run clean && mkdir build && npm run copyhtml",
    "webpack": "webpack",
    "posttsc": "npm run copyhtml"
  },
  "devDependencies": {
    "ts-loader": "^0.8.2",
    "typescript": "1.8.10",
    "webpack": "^1.13.0"
  }
}
